<style>
  .container {
    margin-left: 200px; /* Same as the width of the tab */
    width: calc(100% - 200px);
  }
</style>

<% content_for(:sidebar) do %>
  <div id="mySidenav" class="tab">
    <span class="tablinks" onclick="openCity(event, 'Orders')" id="defaultOpen">Orders</span>
    <span class="tablinks" onclick="openCity(event, 'History')">History</span>
    <span class="tablinks" onclick="openCity(event, 'Meals')">Your meals</span>
  </div>
<% end %>

<div class="container">

  <div class="dash-view tabcontent" id="Orders">
    <%= render 'orders' %>
  </div>

  <div class="dash-view tabcontent" id="History">
    <%= render 'history' %>
  </div>

  <div class="dash-view tabcontent" id="Meals">
    <%= render 'meals' %>
  </div>

</div>
